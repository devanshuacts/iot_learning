extends layout

block content
  h1= title
  p Welcome to #{title}

  h1 Dynamic Content

  if error
    p Error getting dynamic content.
  else
    p The app repo has following counts:

    ul
      li #[strong Things:] !{data.thing_count}
      li #[strong Users:] !{data.user_count}
